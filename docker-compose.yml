services:
  postgres:
    image: postgres:16
    container_name: videomax-postgres
    environment:
      POSTGRES_USER: videomax
      POSTGRES_PASSWORD: videomax
      POSTGRES_DB: videomax
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
